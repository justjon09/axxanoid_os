# Tools config 
TODO add explination of each input

Available Tools: [
    {
        name: Memory Search, 
        script: memory_search/search.py, 
        description: "query local memory by date, topic or key phase"
        query_string_format: "[SEARCH_MEMORY: <query>]"
    },
    # API manager
    # browser
    # vs code
    # read / write / change permissions
    # image viewer
]